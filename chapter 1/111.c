#include <stdio.h>

/*b)
Det som görs innan komplimering, tittar på # bibloteken
Kompilatorn gör om koden vi skrivit till kod datorn förstår (binärt)
Tar alla .obj och lägger ihop dom till en .exe
.c -> .obj -> .exe 
*/

int main(){
    int x = 5*10;
    printf("L\x86t oss g\x94ra lite matte:\n");
    printf("5 * 10 = %i",x);
}